// These need to be objects
// Need to be dynamically created with factory functions or classes

export const taskList = () => {
    taskTitle,
    taskDescription,
    taskPriority,
    isComplete: false,
    editTask(/* Pass in object to edit task details */) {
        
    }
}